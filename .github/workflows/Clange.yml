name: download-clang-release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  download-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Install aria2
        run: sudo apt-get update && sudo apt-get install -y aria2

      - name: Download Clang r584948
        run: |
          aria2c \
            -x 16 \
            -s 16 \
            -k 1M \
            --summary-interval=1 \
            --console-log-level=notice \
            https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/mirror-goog-main-llvm-toolchain-source/clang-r584948.tar.gz \
            -o clang-r584948.tar.gz

      - name: Create Release and Upload
        uses: ncipollo/release-action@v1
        with:
          tag: "clang-r584948-${{ github.run_number }}"
          name: "Clang r584948 #${{ github.run_number }}"
          artifacts: "clang-r584948.tar.gz"
          draft: false
          prerelease: false
